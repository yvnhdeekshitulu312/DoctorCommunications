{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Cors": {
    // Both the deployed frontend origin and localhost dev origins, so this
    // deployed backend can be hit from either while ASPNETCORE_ENVIRONMENT
    // is Production on the server. AllowCredentials() means these must be
    // exact origins — no wildcard is possible.
    "AllowedOrigins": [
      "http://his.alhammadi.med.sa",
      "https://his.alhammadi.med.sa",
      "http://localhost:4200"
    ]
  }
}
