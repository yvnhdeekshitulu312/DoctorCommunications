{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // Fill in with the same SQL Server + credentials DoctorPortalAPI's
    // Web.config already uses (connectionStrings > DBConnectionStringTrans)
    // — new DoctorCommunications_* tables live in that same HIPPO_HIS_UAT database,
    // namespaced by table-name prefix rather than a separate database.
    // Prefer `dotnet user-secrets set ConnectionStrings:DoctorCommunicationsDb "..."`
    // locally instead of pasting the real password into this checked-in file.
    "DoctorCommunicationsDb": "Server=10.134.134.10;Database=HIPPO_HIS_UAT_LATEST;User Id=hippocareuat;Password=Careuat@123$#@!;TrustServerCertificate=True;"
  },
  "Agora": {
    "AppId": "8ba10b49e0264094a4d2c968abdc1e35",
    "AppCertificate": "b0fd6135957e4a5596f583fd6f70838c"
  }
}